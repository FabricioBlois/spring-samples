server:
  port: 8888

encrypt:
  key: IMSYMMETRIC

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/FabricioBlois/spring-config.git
          clone-on-start: true
          force-pull: true
          repos:
            spring:
              patter: spring-*
              uri: https://github.com/FabricioBlois/spring-config.git
              force-pull: true